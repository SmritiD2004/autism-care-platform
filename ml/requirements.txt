# ml/requirements.txt -- ML dependencies
# PRODUCTION: installed in Docker via Dockerfile.backend + Dockerfile.streamlit
# DEV ONLY:   install locally with: pip install -r ml/requirements.txt

# Core data / compute
numpy==1.26.2
pandas==2.1.4
scikit-learn==1.3.2

# Visualisation (used by screening router for SHAP heatmaps)
matplotlib==3.8.2
seaborn==0.13.0

# Video / computer vision (screening.py)
opencv-python>=4.8.0
mediapipe>=0.10.0

# HTTP (dataset download utilities)
requests>=2.28.0

# PyTorch -- used by train.py (ET eye image CNN) and loader.py
torch==2.1.2
torchvision==0.16.2

# TensorFlow / Keras -- used by video-asd-model VGG16+LSTM classifier
# Pin to 2.15 to match video-asd-model expectations
tensorflow==2.15.0

# DEV ONLY -- uncomment locally for notebook experimentation:
# jupyter==1.0.0
# ipykernel
