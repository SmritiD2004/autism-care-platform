# ══════════════════════════════════════════════════════════════════════════════
# NeuroThrive — Environment Variables
# Copy this file to .env and fill in your values.
#
# Two .env files are used:
#   backend/.env  → local development (uvicorn runs from backend/)
#   .env          → project root (Docker Compose reads this)
#
# For local dev, only backend/.env is needed.
# For Docker, only the root .env is needed.
# ══════════════════════════════════════════════════════════════════════════════


# ── Postgres — Docker Compose ─────────────────────────────────────────────────
# These are used by docker-compose.yml to configure the postgres:15 container
# and to build the DATABASE_URL injected into the backend service.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=neurothrive


# ── Database URL — Local Dev (backend/.env) ───────────────────────────────────
# Used directly by config.py → SQLAlchemy engine.
# For Docker, this is auto-built from the POSTGRES_* vars above in docker-compose.yml.
# For local dev, set this manually here.

DATABASE_URL=postgresql://postgres:your_secure_password_here@localhost:5432/neurothrive


# ── JWT ───────────────────────────────────────────────────────────────────────
# Generate a secure value with:
#   openssl rand -hex 32

SECRET_KEY=replace_me_with_output_of_openssl_rand_hex_32

# Token algorithm — do not change unless you know what you're doing
ALGORITHM=HS256

# Token lifetime in minutes (default: 1440 = 24 hours)
ACCESS_TOKEN_EXPIRE_MINUTES=1440


# ── CORS ──────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed frontend origins.
# Tighten this in production — remove * from main.py as well.

FRONTEND_ORIGINS=["http://localhost:5173","http://localhost:3000"]
